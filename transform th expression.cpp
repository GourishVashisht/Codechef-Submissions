#include<iostream>
using namespace std;
int main()
{
    int t;
    char infix[402],postfix[402],stack[402];
    cin>>t;
    while(t--)
    {
        for(int i=0;arr[i]!=NULL;i++)
        {
            cin>>infix[i];
        }
        infix[i]=')';
        stack[0]='(';
        if(infix[j]=='(')
            stack[1]='(';
        else if(infix[j]=='0'||infix[j]=='1'||infix[j]=='2'||infix[j]=='3'||infix[j]=='4'||infix[j]=='5'||infix[j]=='6'||infix[j]=='7'||infix[j]=='8'||infix[j]=='9')
            postfix[]=infix[j];
        else if(infix[j]=='+'||infix[j]=='-'||infix[j]=='*'||infix[j]=='/'||infix[j]=='%'||infix[j]=='^')
            while(infix[]!='(')
        {
            if(infix[j])//pop from stack the operator with same or more precedence than this one
        }
        else if(infix[j]==')')
            while(top->stack!='(')
        {
            postfix[]=top->stack;
            if(top->stack=='(')
               extra=top->stack;
        }


    }
}
